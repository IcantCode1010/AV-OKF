# Graph enhancement implementation and verification

Objective: improve visual graph exploration and make graph retrieval useful, evidence-grounded, and explainable for the agent. The broader architecture plan remains in scope; this checklist is not a completion claim.

## Latest agent integration verification

- Wired the previously ignored 3D `autoFocusSelected` option. External selections schedule camera focus after render, wait for async grouping, and track the last selection to avoid repeated camera interruptions. Browser fixture selected a concept via the answer's text list and rendered its focused graph/source excerpt without errors; screenshot `work/answer-external-focus.png`. Targeted lint passes after moving focus work to an animation-frame callback with cleanup.

- Answer-graph source inspection now displays the selected concept's cited excerpt, document/page attribution, and original-source link. Text relationship endpoints are keyboard-accessible buttons, offering evidence inspection independently of canvas interaction. Browser fixture selected the target via its text button and verified matching excerpt/page/link; no page errors and targeted lint passed. The displayed excerpt is the persisted citation excerpt, not the entire source page.

- Added concept-name search for graphs above 80 nodes. The native node picker shows at most 100 matches plus the current selection, reports match counts, and retains direct focus/expansion behavior. Browser check filtered the 2,738-node fixture to `Concept 2737`, selected it, and expanded its group without reported page errors. Targeted lint passed. This improves access to sparse/unlinked concepts without adding factual relationships.

- Reduced-motion preference now disables visible 3D force-simulation ticks as well as animated camera transitions; warmup computes the initial layout before display. Browser emulation confirmed the media query, a nonblank rendered graph, and byte-identical successive screenshots (`work/graph-reduced-motion-a.png`, `work/graph-reduced-motion-b.png`). No page errors; targeted lint passes. Device-wide and all-interaction accessibility validation remains incomplete.

- Corrected answer-graph identity for concepts sharing a source passage. Persisted shared-research connections now carry topic IDs; the UI keys nodes by topic rather than citation and can show two concepts linked to the same cited page. Distinct cited passages are still preferred when available. Five provenance/connection tests and targeted lint pass. Browser fixture verified two distinct selectable concepts sharing page 1 with no reported errors; screenshot `work/shared-page-concepts.png`. Older traces without topic IDs retain citation-based identity.

- Added and ran live `--incoming` answer probe. Session `cmtpb3tlo000001l7c92d1ooe`, message `cmtpb48zr000301l77ksf9cae`: 19.926 seconds, citations to pages 88 and 84, persisted `SOURCE OFF (single) → references → Loss of Both Engine-Driven Generators`. This verifies starting from the target while retaining original assertion direction in the answer graph. Targeted verifier lint passed. It remains one representative incoming-link case, not comprehensive evaluation.

- Added live `--missing` answer probe for a deliberately nonexistent endpoint. Session `cmtpb2m03000001nrm5t0jq9u`, message `cmtpb2xnz000301nrpdnrxz82`: 15.091 seconds, no graph connections, answer explicitly says the endpoint/relationship is absent from provided evidence. The verifier initially required `insufficient_evidence`, but manual inspection showed a valid answered absence question; changed the check to require an absence statement and no invented connection. This does not establish exhaustive corpus absence or broad negative-query quality.

- Added full live answer verifier `scripts/verify-graph-answer-live.mts`. Initial execution exposed two integration gaps: graph requests routed `okf_only` bypassed shared research, and overlapping topic page ranges selected the same citation for both endpoints even when distinct cited passages existed. Shared research now accepts graph-traversal `okf_only` requests when enabled; connection mapping chooses a distinct endpoint pair when available. Five provenance/connection tests and targeted lint pass.
- Real answer persisted successfully in session `cmtpb0815000001oa3vl6y3oh`, message `cmtpb0h1s000301oac7xhne8x`: 11.665 seconds, two inspected citations on QRH pages 88 and 84, one persisted directed `references` connection. The answer describes the document cross-reference rather than providing operating instructions. This is a single successful model-driven case, not a broad quality benchmark. Three verification chat sessions were created in the authorized workspace; the running app was not rebuilt or restarted.

- Ran current source in disposable verification containers using existing service configuration and read-only source mounts; running app was not restarted. Model research creates audit rows and consumes configured provider usage. Two initial runs returned no paths despite direct traversal finding a relationship. Found empty `relationTypes: []` excluded every edge; it now means no filter, with real traversal regression coverage. Added graph direction/count/warning diagnostics and guidance to check both directions before declaring no links.
- Live research run `cmtpaxo3a000001ot9ym7o9i6` completed in 5.88 seconds, two tool calls/three model steps, one inspected passage, one published graph connection, no gaps. Both topic endpoints share page 273; this proves model-driven published-link/source inspection integration but not a two-node cited answer graph. `scripts/verify-graph-research-live.mts` supports direct read-only `--inspect` diagnostics as well as the model run. Full answer synthesis/persistence and a multi-page case remain outstanding.

- Exercised shared research provenance through the actual citation finalizer: evidence IDs survive reordered sparse markers, directed connections use the final numbers, and insufficient-evidence answers produce no answer graph. Four finalization/provenance tests pass. Updated the browser fixture to raw-source research citations; verified document/page node labels, concept relationship text, and source selection with no reported page errors. Screenshot: `work/shared-research-answer-graph.png`. Added page number to the source-link label after visual inspection. This remains fixture verification rather than a model-driven production answer.

- Shared research graph provenance is now revalidated on cache reuse, before research completion, and before chat answer persistence. Current approved/scoped endpoints, titles, source-page mappings, and the original directed published relation must still match; traversal applies database lifecycle checks. Changed graph provenance invalidates cache reuse. Six targeted provenance/cache tests, targeted lint, and production build pass. Database/provider end-to-end execution and race-window handling during final persistence still need verification.

- Shared research now captures accepted published-link tool results and persists discovery provenance only for endpoints matched to selected inspected document pages. Citations carry research evidence IDs through finalization; answer graphs retain connections only when both endpoint evidence IDs survive final citation selection. Concept titles remain in connection details while raw-source nodes show document/page labels. This is discovery provenance, not proof that each cited passage establishes the relationship. Seven projection/provenance tests, targeted lint, and production build pass. Policy bumped to v6 afterward. Actual model-driven end-to-end execution, graph-revision cache invalidation, and updated browser fixture verification remain required.

- Captured read-only database lifecycle records to `work/graph-lifecycle-snapshot.json` and extended the real-file verifier to apply them. Result: 226 permitted/readable topics and 12/12 paths retained without warnings. Added local-only withdrawal probes for a connected seed and its neighbor; initial run passed (341 ms median, 407 ms maximum lookup), followed by tightening neighbor selection to a directly connected endpoint. No live lifecycle records changed. These are separate read-only snapshots, not a transactionally frozen end-to-end production run.

- Refreshed a read-only snapshot of approved exported topics from the running database/filesystem to local `work/graph-published-snapshot.json`. The running application and graph data were not modified. Added `scripts/verify-published-graph-snapshot.mts`, which recreates files in an isolated temporary directory, tests every seed, then checks bidirectional traversal-to-agent projection on connected concepts. Result: 226/226 readable seeds, 11 connected seeds, 12/12 retained paths, zero warnings; median 306 ms, maximum 329 ms per connected lookup. This validates real published-file structure with snapshot lifecycle only, not current database lifecycle or model answer quality. Local snapshot includes source content and is not a public artifact.

- Corrected cached-research stale-source error handling to recognize `knowledge_sources_changed`, the error actually emitted by scope validation. Added explicit policy tests ensuring cancellation, access denial, missing sources, and infrastructure failures still propagate. Added `sourceClassification` to the scope fingerprint because it is used as fallback evidence authority. Two cache-policy tests and targeted research lint pass; concurrent scope changes still fail subsequent guards rather than silently expanding authorization.

- Mobile browser verification at 390×844 exposed excessive toolbar/legend coverage. Added an accessible mobile controls toggle (desktop remains expanded), reduced the mobile explanatory overlay, and verified opening controls, expanding a group, and hiding controls again. The 2,738-node fixture expanded to 246 visible nodes/734 connections with no reported page errors. Screenshot: `work/graph-mobile-expanded.png`. Targeted lint passed. This is viewport emulation, not a physical-device frame-rate benchmark.

- Added reproducible synthetic CPU benchmark `scripts/benchmark-graph-layout.mts`: median grouping/projection time was 2 ms at 228 nodes/912 edges, 17 ms at 2,738/10,952, and 85 ms at 10,000/40,000 (Node v24.13.0, five measured runs after warmup). These are not browser frame-rate or live-data measurements.
- Graphs above 1,000 nodes now group in a dedicated browser worker with loading status, stale-result rejection, worker cleanup, and synchronous fallback when workers are unavailable. The 2,738-node fictional browser scene rendered 15 groups; the worker resource loaded and no page errors were reported. Screenshot: `work/graph-large-worker.png`. Targeted lint and production build passed before the final worker-termination cleanup and preview-fixture edit. Live-data/mobile performance verification remains outstanding.

- Scoped reverse indexing now reads the explicit authorized-file inventory instead of enumerating unrelated bundle directories. It skips symlinked ancestors/files, rejects noncanonical paths, retains lifecycle reads and output budgets, and omits out-of-scope targets from the index. A filesystem test verifies direct inventory behavior, junction exclusion, and file-budget warnings. All 12 traversal tests and targeted lint pass. Live-bundle latency benchmarking is still outstanding.

- Actual traversal-to-agent integration uncovered and fixed an adapter contract mismatch: `concepts` contains discoveries, not seeds. Traversal now returns explicit inspected file identities, allowing the adapter to retain validated starting concepts without treating every authorized topic as inspected. A real filesystem fixture proves incoming A→B survives when B is the seed, and isolated validated seeds retain their source pointers. Fifteen traversal/adapter tests and targeted lint pass. Policy bumped to `graph-research-v5` to avoid reusing research with dropped paths. Scoped reverse-index performance work remains pending; this correctness issue took priority.

- Research source evidence is now registered only after its serialized tool response fits the remaining character budget. Aborted reads are checked before admission. Previously, a rejected response could remain in the evidence map and enter a partial result without reaching the research model. Two budget-boundary regression tests and the two projection tests pass; targeted lint and production build pass. Research policy is now `graph-research-v4` to exclude cached results from the previous admission behavior (version-only update followed the build).

- Fixed shared research synthesis fallback: an empty successful or budget-limited run no longer restores legacy discovery snippets. The pure research projection replaces citations/evidence even when empty, deduplicates inspected evidence IDs, preserves full passages for synthesis, and clears stale OKF mode/conflict flags. `sourcesRead` now lists only inspected passages for this path. Two regression tests cover empty/partial runs and inspected-only projection; the combined retrieval/answer/projection suites pass 51 tests, and targeted lint passes. Shared-agent connection provenance remains separate outstanding work.

- Added persisted answer connections for the deterministic retrieval path, projected after final citation numbering. Only edges whose two endpoints remain cited in the same bundle are retained; withdrawn endpoints are excluded. Three regression tests cover numbering, duplicate paths, removed endpoints, bundle isolation, and absence of invented context.
- Chat now offers an on-demand 3D/2D graph of those cited concepts, source navigation, and a text relationship list. Browser verification on the fictional preview exercised opening and concept selection; no page errors were reported. Screenshot: `work/chat-answer-graph.png`. Targeted lint and production build pass (test auth disabled only in build process). The final caption/header adjustment followed the build.
- Shared research currently replaces legacy concept evidence with inspected source passages, so it does not yet populate this answer graph. Capturing and validating that research provenance against final citations remains required; no claim of complete agent integration or deployment is made.

- Re-polled production build session 84768: terminal exit code 0. This verifies the earlier build with test authentication disabled for that process, before the result-adapter change below; it is not a deployment.
- The published-link research tool now uses a tested result adapter. It excludes paths with unknown or uninspected endpoints, checks path shape, retains original assertion direction during incoming traversal, and exposes only explicit topic/source-pointer fields. Isolated inspected concepts remain available even without a relationship path.
- Four adapter regression tests and targeted ESLint pass. Database/provider end-to-end evaluation and source-revision validation remain outstanding.

## Implemented, with targeted verification

- Published traversal uses multi-source breadth-first expansion, retains converging paths, and has node/edge budgets. Six traversal tests pass.
- Graph evidence selection has a separate eight-concept budget, ranks connected evidence, and selects complete paths before filling remaining space. Four selection/integration tests pass, including four direct hits plus a connected result reaching the answer prompt.
- The answer prompt receives typed connection context with explicit source-verification and non-transitivity instructions. Existing answer tests pass (24 tests total across these three suites).
- Both explorer modes use a client-only 3D renderer by default, with a 2D switch, node focus, relationship filtering, selected/hover labels, overview labels, and a type legend. Canvas labels avoid HTML tooltips.
- Development-only `/graph-preview` uses fictional data and returns not found outside development. Browser checks covered node selection, relationship filtering (96 to 48 links), reset, 2D switching, mobile layout, and reported page errors. Screenshots: `work/graph-preview-desktop.png`, `work/graph-preview-mobile.png`.
- Targeted lint passes. The full TypeScript check has numerous errors in existing test fixtures; it is not a passing release gate. A filtered check reported no errors in the changed graph UI, graph selection, chat answer, or chat retrieval implementation files.

## Implemented, awaiting stronger verification

Latest increment:

- Complete path members are reserved during multi-bundle ranking and adaptive retry selection. Context for discarded paths is pruned using bundle-scoped file identities. Graph provenance is tracked per evidence item when produced by the new retrieval path.
- Published retrieval now supports incoming/outgoing/both traversal and relationship filters, with original assertion direction retained. The request-local reverse index has file/entry/edge limits and cooperative elapsed-time checks. Explicit file allowlists prevent traversal into unselected source topics.
- The shared research tool `follow_published_links` is wired to this traversal with authorized approved-topic files, topic identifiers and source-page pointers. Its database/provider integration still needs runtime evaluation.
- Forty-five retrieval tests pass across traversal, graph selection and the full chat retrieval suite. A separate layer test verifies published/evidence/review separation and retained provenance. Targeted lint passes; filtered type checking of the retrieval changes reported no errors.
- The entity explorer now separates published relationships, source evidence, review candidates and all connections. Its node/edge projections are memoized so selection does not restart the spatial simulation. Browser checks exercised all three layers on the fictional entity fixture with matching status detail and no reported page errors. Screenshot: `work/graph-entity-layers.png`.

- Shared agent `follow_links` supports incoming/outgoing/both candidate lookup, relation filtering, original endpoints, and evidence-page pointers; both endpoints are constrained to the authorized document scope. Invalid candidate statuses are excluded. Native-file targets remain outgoing only.
- Research policy version is `graph-research-v3` so old cached research does not bypass the changed tools.
- Larger graph evidence budgets are applied during multi-bundle merging and adaptive retries. Path-aware selection now has regression coverage; source revision changes and retry deduplication of revised metadata still need deeper verification.

## Remaining work

Latest visualization and live-data findings:

- Added deterministic Louvain connectivity groups, reversible group expansion, keyboard-accessible group selection, and an all-nodes mode. Groups are explicitly navigation aids, not semantic assertions. Unlinked concepts are collected in a separately labeled browsing group. The published explorer now opens on the whole graph; 3D views above 80 nodes start grouped.
- Three community tests cover stable grouping, restoration of every directed assertion on full expansion, and unlinked-node handling. Together with the explorer suite, 16 tests passed before the isolated-node browsing adjustment; the three community tests and targeted lint passed again afterward.
- Browser fixture: 48 nodes reduced to six groups; expanding a 12-member group produced 17 visible nodes. No page errors reported. Screenshots: `work/graph-groups.png`, `work/graph-groups-expanded.png`.
- Read-only inventory of the running database found 2,738 canonical entities; entity relation candidates included 632 unresolved, six published, and 19 filtered. Published-concept inspection of the populated bundle found 228 nodes, six edges, 217 isolated nodes, and zero explorer validation issues. An initial probe of the first active bundle found an empty bundle; the populated-bundle measurement used an approved topic to identify its bundle.
- These measurements identify a substantive next task: investigate why grounded relationship candidates fail target resolution and evaluate improvements through the existing verification/publication pipeline. Do not infer or publish links merely to make the visual graph denser. Current live data was not mutated.

1. Complete shared graph query services, including incoming published-link lookup, stable identities, typed path results, endpoint authorization, time/token budgets, and truthful truncation diagnostics.
2. Preserve complete, scoped evidence paths through multi-bundle ranking and retries. Ensure graph mode is claimed only when graph-derived evidence survives final selection. Carry stable assertion/endpoint references instead of relying on display names.
3. Evaluate and test the shared agent tools on scoped real-source fixtures, including conflicting/revised sources, ambiguous entities, incoming dependencies, access boundaries, and unanswerable questions.
4. Define and enforce relationship semantics, assertion provenance, applicability, lifecycle invalidation, and entity merge/split behavior. Establish authoritative data ownership and reproducible graph projections.
5. Add meaningful community/group exploration and progressive expansion, knowledge/evidence/review layer separation, source-backed relationship inspection, and answer-specific connection views. Verify selection, camera behavior, reduced motion, device fallback, keyboard navigation, and dense graphs in both real explorer modes.
6. Benchmark existing storage against a dedicated graph store before choosing infrastructure. Measure answer correctness, citation support, recall, irrelevant context, latency, cost, and largest-bundle rendering on representative hardware.
7. Finish production verification, integrity checks, rebuild/recovery procedures, monitoring and rollback. Do not claim production deployment or complete goal fulfillment from the development preview or synthetic tests.

## Workspace notes

## Target-resolution increment

- Extracted a testable destination resolver that excludes source-topic anchor mentions from destination ambiguity, recognizes unique explicitly quoted multiword names within longer approved headings, preserves ambiguity rather than choosing a fuzzy winner, and records actual resolution strategy (exact name, accepted alias, anchor, or heading phrase).
- Candidates continue through the existing relationship verifier/publication settings. No live candidates were changed or published by this increment.
- Read-only evaluation snapshot: 632 unresolved assertions, 226 approved exported topics. Former resolver identified seven eligible destinations; the new resolver identifies 37. These are destination matches, not verified relationships. 232 assertions lack an approved exported source topic. Outcomes across all assertions: 29 heading-phrase, nine anchor, five exact-name, 545 no-match, 38 ambiguous, six self-reference. Snapshot/report remain local in `work/graph-target-resolution-input.json` and `work/graph-target-resolution-report.json`.
- Added `scripts/eval-graph-target-resolution.mts` for repeatable read-only comparison. Twelve resolver/extraction tests and targeted lint pass. Worker expansion now applies its cap to queued verifications rather than slicing out candidates before source/duplicate checks, and avoids overwriting an existing projection's originating assertion. Worker/database integration of these changes remains to be validated before rollout.

## Workspace notes (continued)

Retry integration verification: duplicate concept hits now retain the most recent evidence and newly discovered graph context instead of discarding the retry hit. A changed excerpt invalidates older path context on related concepts that were not re-inspected. Regression tests verify both behaviors without mutating input results. The 28-test chat retrieval suite, 10-test graph evidence suite, and targeted lint pass. This is excerpt-level invalidation; content-hash/revision-aware end-to-end source validation remains part of the broader release work.

Traversal integration: starting concepts are validated before reverse-index discovery. Indexing receives half the remaining cooperative time budget, preserving traversal time, and reports `graph_index_time_budget_exhausted` separately. An inaccessible seed no longer triggers a bundle scan. The combined graph/retrieval/answer/explorer/resolver suites pass 91 tests. A normal production build passed compilation and its TypeScript phase but local default test-login settings stopped page-data collection. A follow-up build disables test authentication for that process only; saved environment and the running Docker application remain unchanged.

The checkout contains extensive pre-existing shared knowledge and topic-builder work. Preserve it. Chat has both a deterministic published-OKF path and a newer shared research path; neither can be treated as the sole integration point. The running Docker application on port 3000 has not been rebuilt or changed by this graph work. Development verification used port 3001.
